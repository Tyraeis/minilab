machine:
  install:
    diskSelector:
      type: sd
---
apiVersion: v1alpha1
kind: VolumeConfig
name: EPHEMERAL
provisioning:
  diskSelector:
    match: disk.transport == 'usb'
  minSize: 2GB
  grow: true

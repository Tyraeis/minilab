apiVersion: k8s.mariadb.com/v1alpha1
kind: Database
metadata:
  name: crowdsec
spec:
  mariaDbRef:
    name: mariadb
    namespace: mariadb
---
apiVersion: k8s.mariadb.com/v1alpha1
kind: User
metadata:
  name: crowdsec
spec:
  mariaDbRef:
    name: mariadb
    namespace: mariadb
  passwordSecretKeyRef:
    name: mariadb-secret
    key: password
  host: "%"
  cleanupPolicy: Delete
---
apiVersion: k8s.mariadb.com/v1alpha1
kind: Grant
metadata:
  name: crowdsec
spec:
  mariaDbRef:
    name: mariadb
    namespace: mariadb
  privileges:
    - "ALL"
  database: crowdsec
  table: "*"
  username: crowdsec
  host: "%"
